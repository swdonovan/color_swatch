const $ = require('jquery')
import './stylesheets/styles.scss'
import COLORS from './data/colors'
import WordListener from './event_listeners/word_listener'

$(document).ready(() => {
  WordListener
});
